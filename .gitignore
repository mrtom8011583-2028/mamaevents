# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate.log

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch.json and tasks.json with launch
# configurations and task definitions specific to your project. These files can
# be useful to share if you are working with a team. However, for most projects,
# the .vscode folder is not shared. a common consensus is to not share the
# .vscode folder.
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place separate folders in android/ for each module
# which has its own build.gradle file. You should not share these folders.
android/app/debug/
android/app/profile/
android/app/release/

# Firebase related
.firebase/
firebase-debug.log
firestore-debug.log
pubsub-debug.log
ui-debug.log

# Environment variables
.env
.env.*
lib/firebase_options.dart
